import { Text, View } from "react-native";
import Form from "../components/Form";

const SignUp = () => {
  return (
    <View>
      <Text>"Sign up"</Text>
      <Form type={"signup"} />
    </View>
  );
};

export default SignUp;
