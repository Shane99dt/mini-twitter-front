import { Text, View } from "react-native";
import Form from "../components/Form";

const Login = () => {
  return (
    <View>
      <Text>"Login"</Text>
      <Form type={"login"} />
    </View>
  );
};

export default Login;
