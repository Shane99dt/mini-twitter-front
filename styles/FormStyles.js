import { StyleSheet } from "react-native";

const FormStyles = StyleSheet.create({
  input: {
    paddingVertical: 5,
    borderWidth: 1,
    marginBottom: 5,
    paddingHorizontal: 5,
  },

  container: {
    flex: 1,
  },
});

export default FormStyles;
