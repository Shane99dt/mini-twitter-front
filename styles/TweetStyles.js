import { StyleSheet } from "react-native";

const TweetStyles = StyleSheet.create({
  box: {
    borderWidth: 1,
    borderColor: "#00f",
    paddingHorizontal: 5,
    paddingVertical: 2,
    marginBottom: 5,
    borderRadius: 5,
  },

  description: {
    color: "red",
  },
});

export default TweetStyles;
